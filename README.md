This repo is for experimenting with different preprocessing techniques and models before incorporating them into a heavier preprocessing pipeline. You can use it too — just replace the arguments in the `create_dataset` function call.
It works with Russian only (the GigaAM model is used).
